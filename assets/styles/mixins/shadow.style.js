/**
 * =Shadow
************************************************************/

import styled, {css} from "styled-components";

export default function () {
  return css`
    box-shadow: 0 10px 6px rgba(0, 0, 0, .26);
  `;
}
